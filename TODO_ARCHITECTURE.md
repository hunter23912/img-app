# 架构优化 TODO

目标：在不扩大项目复杂度的前提下，保持图片生成应用易于维护、扩展和发布。

## 当前结构

后端已经完成按职责分包，启动方式和 API 保持不变：

~~~text
backend/
├── main.go
└── internal/
    ├── config/       配置加载和默认值
    ├── store/        SQLite、迁移和持久化领域操作
    ├── provider/     中转站图片接口代理
    ├── imageops/     图片来源校验、解码、下载和格式转换
    ├── history/      内存历史记录
    ├── httpapi/      HTTP 路由、handler、响应和中间件
    └── logging/      日志初始化
~~~

main.go 只负责配置加载、数据库和依赖初始化、恢复图片来源以及启动 HTTP 服务。具体业务分别位于对应的 internal 包中。

## 已完成

- 前端已拆分 API、组件、hooks、类型和工具模块。
- 已支持文生图和图编辑。
- 已支持三个固定图片模型：
  - gpt-image-2
  - grok-imagine-image-2.0
  - gemini3.1-flash-image
- 已支持按中转站配置隔离的自定义模型手动添加和删除。
- 已支持多个中转站配置的创建、编辑、切换和删除。
- 已支持 SQLite 持久化提示词预设，并兼容旧版 localStorage 预设迁移。
- 已支持 SQLite 图片历史、分页、base64 图片按需读取和删除。
- 已支持 PNG 原图下载和 JPG 转换下载。
- 已完成后端基础分包，并保留 cd backend && go run .、后端构建命令和现有 API。
- 已补充配置、历史、模型、数据库和中转站调用相关测试。

## 后续计划

### 1. 前端页面拆分

当前 src/App.tsx 仍负责主导航、表单状态、提交流程和结果区域组装。后续可以在功能继续增加时拆分为：

- 生图页面。
- 图编辑页面。
- 配置页面。
- 结果和历史通用组件。

拆分应以降低 App.tsx 状态复杂度为目标，不提前引入路由或重量级状态管理库。

### 2. 统一错误和输入校验

当前部分错误处理仍由各 handler 单独完成，后续可以：

- 增加统一的错误类型和 HTTP 状态映射。
- 统一 JSON 请求体、文件大小和图片格式限制。
- 统一前端错误信息和后端错误信息的中文提示。
- 补充路由级集成测试，覆盖配置、模型和历史接口。

### 3. 图片和中转站能力

- 根据实际中转站差异，逐步补充可配置的请求参数。
- 评估生成任务并发限制和更明确的超时处理。
- 必要时增加外部图片 URL 的过期或失效提示。
- 继续保持 provider 与 HTTP 层分离，避免把中转站协议细节放进 handler。

### 4. 部署

- 补充 Nginx 配置示例。
- 补充 systemd 或其他进程管理方式的示例。
- 明确生产环境的 API 认证、访问控制、限流和数据库备份要求。

## 明确暂不做

- 暂不自动请求中转站的 /v1/models；当前模型列表由三个固定模型和手动添加的自定义模型组成。
- 暂不增加用户系统、登录、计费和复杂权限。
- 暂不引入重量级 Go 或前端依赖，除非现有标准库和组件已经无法满足需求。

## 验收标准

每次涉及代码或目录调整时，至少确认：

~~~bash
cd backend
go test ./...
go vet ./...
go build -o /tmp/img-app-backend .
cd ..
pnpm lint
pnpm build
git diff --check
~~~

同时确认：

- cd backend && go run . 仍可启动后端。
- 原有文生图、图编辑、配置、模型、预设和历史功能没有回退。
- 数据库文件没有被删除或覆盖。
- README.md 与实际目录、启动方式和接口保持一致。
